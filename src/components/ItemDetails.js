import React from "react";

const ItemDetails = (props) => {
    console.log(props);
    return (
        <div>
            <h1>Item Details Page</h1>
        </div>
    );
};

export default ItemDetails;